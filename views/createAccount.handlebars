<section class="section-padding pt-0">
  <div class="container">
    <div class="signup-gate">
      <section class="signup-gate__form">
        <header class="signup-gate__header">
          <span class="section-heading__eyebrow">Create account</span>
          <h1 class="signup-gate__title">Set up your GreenUP profile</h1>
          <p class="signup-gate__lead">
            Unlock tailored recommendations, seamless checkout, and wellness guidance curated just for you.
          </p>
        </header>
        <div class="alert alert-danger" role="alert" data-alert="standalone-signup"></div>
        <form id="standalone-signup-form" class="needs-validation" data-standalone-signup novalidate>
          <div>
            <label class="form-label" for="ssu-name">Full name</label>
            <input
              id="ssu-name"
              class="form-control"
              name="name"
              type="text"
              autocomplete="name"
              required
              minlength="2"
            />
            <div class="invalid-feedback">Please enter your name (2 characters minimum).</div>
          </div>
          <div>
            <label class="form-label" for="ssu-email">Email address</label>
            <input
              id="ssu-email"
              class="form-control"
              name="email"
              type="email"
              autocomplete="email"
              required
            />
            <div class="invalid-feedback">We need a valid email to create your account.</div>
          </div>
          <div>
            <label class="form-label" for="ssu-password">Password</label>
            <input
              id="ssu-password"
              class="form-control"
              name="password"
              type="password"
              autocomplete="new-password"
              required
              minlength="8"
            />
            <div class="form-text">Use at least 8 characters including upper, lower, and a number.</div>
            <div class="invalid-feedback">Please choose a stronger password.</div>
          </div>
          <div class="signup-gate__actions">
            <button type="submit" class="btn btn-success" data-submit>Create account</button>
            <a class="btn btn-outline-success" href="/">Back to sign in</a>
          </div>
        </form>
      </section>

      <aside class="signup-gate__perks">
        <span class="signup-gate__perks-eyebrow">Member perks</span>
        <h2 class="h4 fw-semibold">Set wellness goals and let GreenUP help you reach them</h2>
        <ul class="signup-gate__list">
          <li><i class="fas fa-clock"></i> Flexible deliveries that adapt to your schedule and pantry.</li>
          <li><i class="fas fa-gift"></i> Early access to limited releases and community tasting events.</li>
          <li><i class="fas fa-heartbeat"></i> Nutrition insights that turn good intentions into sustainable habits.</li>
        </ul>
      </aside>
    </div>
  </div>
</section>
<script defer src="/js/profile.js"></script>
